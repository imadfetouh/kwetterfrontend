// const gateway = 'http://52.158.167.107:8080/'
const gateway = 'http://localhost:8080/'

const urls = {
    signIn: gateway + 'signin',
    signUp: gateway + 'signup',
    addTweet: gateway + 'tweet'
}

export default urls;